# Example env
PORT=3000
# Optionally use Postgres as a DB server instead of the default sqlite file.
# Example: DATABASE_URL=postgresql://postgres:example@db:5432/srimaata
DATABASE_URL=

# Razorpay keys (server-side) — set these to enable payments
RZP_KEY_ID=
RZP_KEY_SECRET=
